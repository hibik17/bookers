<div class="row m-5">
  <%= form_with url: search_by_calender_path, local: false do |f| %>
    <%= f.date_field :search_date %>
    <%= f.hidden_field :user_id, :value => user.id %>
    <%= f.submit "search", class: "btn btn-primary" %>
  <% end %>
</div>
